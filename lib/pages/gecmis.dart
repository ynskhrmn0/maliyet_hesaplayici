import 'package:flutter/material.dart';

class Gecmis extends StatelessWidget {
  const Gecmis({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text("Merhaba"),
    );
  }
}
